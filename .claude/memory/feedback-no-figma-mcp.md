---
name: feedback-no-figma-mcp
description: Never call the Figma MCP — all design info lives in local markdown files
metadata:
  type: feedback
---

Do NOT call the Figma MCP tools for this project.

**Why:** All design information is captured in `figma-design-system.md` and `design-tokens.md` in the project root. The user has explicitly said not to call Figma MCP.

**How to apply:** When referencing design specs, read those two local files. Never call `mcp__claude_ai_Figma__*` tools.
