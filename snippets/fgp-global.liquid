<style>
/* ─────────────────────────────────────────────
   FGP GLOBAL — loads on every FGP section render
   All colour values reference CSS vars from stressbiotic.css
───────────────────────────────────────────── */

/* ── Header ─────────────────────────────── */
.header-wrapper,
.section-header .header-wrapper {
  background: var(--fgp-picton-blue) !important;
  border-bottom: none !important;
}

/* Desktop: logo left | nav centered | icons right
   Override Dawn's middle-left (auto auto 1fr) to match middle-center (1fr auto 1fr)
   but keeping heading on the left grid-area, not swapped to center */
@media screen and (min-width: 990px) {
  .header--middle-left {
    grid-template-areas: 'heading navigation icons' !important;
    grid-template-columns: 1fr auto 1fr !important;
  }
}

.header__heading-link,
.header__heading-link:visited,
.list-menu__item--link,
.list-menu__item--link:visited {
  color: #ffffff !important;
  font-family: var(--fgp-font-primary) !important;
  font-size: 16px !important;
  font-weight: 400 !important;
  letter-spacing: 0.6px !important;
  text-decoration: none !important;
  text-transform: uppercase !important;
}

/* Active / current page link */
.list-menu__item--link.header__active-menu-item {
  font-weight: 700 !important;
  text-decoration: underline !important;
  text-underline-offset: 4px !important;
}

.header__icon,
.header__icon svg,
.header__icon .svg-wrapper,
.header__icon .svg-wrapper svg {
  color: #ffffff !important;
  fill: #ffffff !important;
  stroke: #ffffff !important;
}

.cart-count-bubble {
  background: var(--fgp-dark-cornflower-blue) !important;
  color: var(--fgp-white) !important;
}

/* ── Cart drawer — navy system, matches nav panel ── */
.cart-drawer .drawer__inner,
cart-drawer .drawer__inner {
  background: var(--fgp-navy, #234092) !important;
  backdrop-filter: none !important;
}

/* All text white */
.cart-drawer .drawer__inner .drawer__heading,
.cart-drawer .drawer__inner .cart__empty-text,
.cart-drawer .drawer__inner .cart__login-title,
.cart-drawer .drawer__inner h2,
.cart-drawer .drawer__inner h3,
.cart-drawer .drawer__inner p,
.cart-drawer .drawer__inner span:not(.cart-count-bubble),
.cart-drawer .drawer__inner li,
.cart-drawer .drawer__inner td,
.cart-drawer .drawer__inner label {
  color: #ffffff !important;
  font-family: var(--fgp-font-primary, 'Poppins', sans-serif) !important;
}

.cart-drawer .drawer__inner .cart__login-paragraph,
.cart-drawer .drawer__inner .cart__login-paragraph p,
.cart-drawer .drawer__inner .cart-item__details .price--on-sale,
.cart-drawer .drawer__inner .visually-hidden {
  color: rgba(255,255,255,0.65) !important;
}
.cart-drawer .drawer__inner .cart__login-paragraph a {
  color: var(--fgp-blue, #45bfef) !important;
  text-decoration: underline !important;
  text-underline-offset: 2px !important;
}

/* Close button — white X */
.cart-drawer .drawer__inner .drawer__close,
.cart-drawer .drawer__inner .drawer__close svg,
.cart-drawer .drawer__inner .drawer__close path {
  color: #ffffff !important;
  stroke: #ffffff !important;
  fill: none !important;
}
.cart-drawer .drawer__inner .drawer__close {
  background: none !important;
  border: none !important;
  border-radius: 50% !important;
  transition: background 0.2s !important;
}
.cart-drawer .drawer__inner .drawer__close:hover {
  background: rgba(255,255,255,0.12) !important;
}

/* Divider lines */
.cart-drawer .drawer__inner .drawer__header,
.cart-drawer .drawer__inner .cart-drawer__footer {
  border-color: rgba(255,255,255,0.18) !important;
}

/* Prices — gold accent */
.cart-drawer .drawer__inner .price,
.cart-drawer .drawer__inner .price__regular,
.cart-drawer .drawer__inner .cart-item__price-wrapper .price-item {
  color: var(--fgp-gold, #fed304) !important;
}

/* "Continue shopping" button — white outline pill */
.cart-drawer .drawer__inner .button {
  font-family: var(--fgp-font-primary, 'Poppins', sans-serif) !important;
  background: transparent !important;
  color: #ffffff !important;
  border: 1.5px solid rgba(255,255,255,0.5) !important;
  border-radius: 200px !important;
  font-weight: 600 !important;
  letter-spacing: 0.4px !important;
  transition: background 0.2s, border-color 0.2s !important;
}
.cart-drawer .drawer__inner .button:hover {
  background: rgba(255,255,255,0.1) !important;
  border-color: #ffffff !important;
}

/* Checkout button — gold CTA */
.cart-drawer .drawer__inner .cart__checkout-button,
.cart-drawer .drawer__inner button[name="checkout"] {
  background: var(--fgp-gold, #fed304) !important;
  color: var(--fgp-navy, #234092) !important;
  border: none !important;
  border-radius: 200px !important;
  font-family: var(--fgp-font-primary, 'Poppins', sans-serif) !important;
  font-weight: 700 !important;
  letter-spacing: 0.5px !important;
}
.cart-drawer .drawer__inner .cart__checkout-button:hover,
.cart-drawer .drawer__inner button[name="checkout"]:hover {
  opacity: 0.9 !important;
}

/* Subtotal label */
.cart-drawer .drawer__inner .totals__subtotal,
.cart-drawer .drawer__inner .totals__subtotal-value {
  color: #ffffff !important;
  font-family: var(--fgp-font-primary, 'Poppins', sans-serif) !important;
  font-weight: 600 !important;
}

/* Cart item product title */
.cart-drawer .drawer__inner .cart-item__name,
.cart-drawer .drawer__inner .cart-item__name:visited {
  color: #ffffff !important;
  font-weight: 500 !important;
}

/* Quantity stepper */
.cart-drawer .drawer__inner .quantity {
  border-color: rgba(255,255,255,0.3) !important;
  background: transparent !important;
  border-radius: 200px !important;
}
.cart-drawer .drawer__inner .quantity__button,
.cart-drawer .drawer__inner .quantity__input {
  color: #ffffff !important;
  background: transparent !important;
}

/* Remove button */
.cart-drawer .drawer__inner .button--tertiary,
.cart-drawer .drawer__inner cart-remove-button a,
.cart-drawer .drawer__inner .cart-item__quantity-wrapper .button--tertiary {
  color: rgba(255,255,255,0.5) !important;
}
.cart-drawer .drawer__inner .button--tertiary:hover {
  color: #ffffff !important;
}

/* ── Mobile drawer / slide menu ──────────── */
.menu-drawer__inner-container,
.menu-drawer {
  background: var(--fgp-picton-blue) !important;
}

.menu-drawer__navigation-items a,
.menu-drawer__navigation-items a:visited,
.menu-drawer__close-button {
  color: var(--fgp-dark-cornflower-blue) !important;
  font-family: var(--fgp-font-primary) !important;
  font-size: 16px !important;
  font-weight: 400 !important;
  letter-spacing: 0.6px !important;
}

.menu-drawer__navigation-items .list-menu__item {
  border-bottom: 1px solid var(--fgp-nav-divider);
}

/* ── Product page — force section background to navy ── */
.section-main-product,
.section-featured-product,
.section-main-product > *,
.section-featured-product > *,
.section-main-product [class*="color-"],
.section-featured-product [class*="color-"],
.section-main-product .gradient,
.section-featured-product .gradient,
product-info {
  background-color: var(--fgp-dark-cornflower-blue) !important;
}

/* ── Product page — typography & font overrides ── */
.product,
.product__info-wrapper,
.product__media-wrapper,
.product-section {
  font-family: var(--fgp-font-primary) !important;
}

.product__media-wrapper,
.product__media-container,
.product__media,
.product__media-gallery,
.product-media-container,
.product__media .media,
.product__media deferred-media,
media-gallery,
media-gallery > *,
.product-single__photos,
.product-single__photo {
  background-color: var(--fgp-dark-cornflower-blue) !important;
  border-radius: 0 !important;
}

/* Product media is set to media_fit: contain so the bottle is never cropped.
   Contained packshots need a light canvas — on the navy section they would
   otherwise float on dark blue. */
.product__media .media,
.product__media-container .media {
  background-color: var(--fgp-white, #fff) !important;
  border-radius: 16px !important;
  overflow: hidden;
}

.product__media img,
.product__media-container img {
  padding: 24px;
  box-sizing: border-box;
}

/* Thumbnails follow the same treatment so the strip matches the main image */
.thumbnail__badge,
.product__media-toggle {
  border-radius: 12px;
}

.thumbnail img {
  background-color: var(--fgp-white, #fff) !important;
  padding: 6px;
  box-sizing: border-box;
}

/* ── Product cards (related products, collections, search) ──
   Same packshot problem as the product page: Dawn's card CSS uses cover,
   which crops the cap and base off the bottles. */
.card__media .media img,
.card .media img {
  object-fit: contain !important;
  background-color: var(--fgp-white, #fff);
}

.card__inner,
.card__media {
  background-color: var(--fgp-white, #fff) !important;
}

.card__media .media {
  padding: 14px;
  box-sizing: border-box;
}

/* Card buttons picked up the scheme's purple; bring them back to brand navy */
.card__inner ~ .card__content .button,
.card-information .button,
.card__content .quick-add__submit,
product-form .quick-add__submit {
  background: var(--fgp-dark-cornflower-blue, #234092) !important;
  color: var(--fgp-white, #fff) !important;
  border-radius: var(--radius-pill, 200px) !important;
  font-family: var(--fgp-font-primary, 'Poppins', sans-serif) !important;
  font-weight: 600 !important;
}

.card__heading a,
.card__heading a:visited {
  color: var(--fgp-dark-cornflower-blue, #234092) !important;
  font-family: var(--fgp-font-primary, 'Poppins', sans-serif) !important;
}

/* The vendor line repeats "FEELGOODPHARMA" under every card — every product
   is ours, so it carries no information */
.card__content .caption-with-letter-spacing.light {
  display: none;
}

.product__text.caption-with-letter-spacing {
  font-family: var(--fgp-font-primary) !important;
  color: var(--fgp-hero-text-muted) !important;
  font-size: 13px !important;
  letter-spacing: 1.5px !important;
}

.product__title {
  font-family: var(--fgp-font-primary) !important;
  font-weight: 600 !important;
  color: var(--fgp-white) !important;
  text-transform: none !important;
  letter-spacing: normal !important;
}

.price__container .price-item,
.price .price-item {
  font-family: var(--fgp-font-primary) !important;
  color: var(--fgp-white) !important;
  font-weight: 600 !important;
}

.product__description a,
.product__policies a,
.product__policies p {
  font-family: var(--fgp-font-primary) !important;
  color: rgba(255, 255, 255, 0.7) !important;
}

.section-main-product .link,
.section-featured-product .link {
  color: rgba(255, 255, 255, 0.7) !important;
  text-decoration-color: rgba(255, 255, 255, 0.4) !important;
}

.product-form__input label,
.variant-picker .form__label,
fieldset.js-contents legend,
.product-form__input .form__label {
  font-family: var(--fgp-font-primary) !important;
  color: var(--fgp-white) !important;
  font-weight: 600 !important;
}

.swatch-input ~ .swatch-label,
.button-swatch,
.variant-button,
.product-form__input input[type=radio] ~ label {
  font-family: var(--fgp-font-primary) !important;
  border-color: var(--fgp-dark-cornflower-blue) !important;
  color: var(--fgp-dark-cornflower-blue) !important;
  background: var(--fgp-white) !important;
  border-radius: var(--radius-pill) !important;
}

.product-form__input input[type=radio]:checked ~ label {
  background: var(--fgp-dark-cornflower-blue) !important;
  color: var(--fgp-white) !important;
  border-color: var(--fgp-dark-cornflower-blue) !important;
}

.quantity__label,
.product-form__input--quantity label {
  font-family: var(--fgp-font-primary) !important;
  color: var(--fgp-white) !important;
  font-weight: 600 !important;
}

.quantity {
  border-radius: var(--radius-pill) !important;
  border-color: var(--fgp-orange-yellow) !important;
  background: var(--fgp-orange-yellow) !important;
}

.quantity__button,
.quantity__input {
  font-family: var(--fgp-font-primary) !important;
  color: var(--fgp-dark-cornflower-blue) !important;
  background: transparent !important;
  font-weight: 700 !important;
}

.product-form__input--selling-plan legend,
.selling-plan-group__label,
.selling-plan-group-option__label {
  font-family: var(--fgp-font-primary) !important;
  color: var(--fgp-white) !important;
  font-weight: 600 !important;
}

.selling-plan-group__radio-label,
.selling-plan-group-option__label,
.selling-plan-group-option__value {
  font-family: var(--fgp-font-primary) !important;
  color: var(--fgp-white) !important;
}

.product-form__input--selling-plan .form__label {
  color: var(--fgp-white) !important;
  font-weight: 600 !important;
}

.product-form__submit,
button.product-form__submit,
.product-form__submit[disabled],
.product-form__submit:disabled,
.product-form__submit[aria-disabled="true"],
.product-form__submit.button--secondary,
.product-form__submit.button {
  font-family: var(--fgp-font-primary) !important;
  background: var(--fgp-deeper-blue) !important;
  color: var(--fgp-white) !important;
  border: none !important;
  outline: none !important;
  box-shadow: none !important;
  border-radius: var(--radius-pill) !important;
  font-weight: 600 !important;
  letter-spacing: 1px !important;
  transition: opacity 0.2s !important;
  opacity: 1 !important;
}

.product-form__submit:hover {
  opacity: 0.88 !important;
}

.shopify-payment-button__button,
.shopify-payment-button__button--unbranded {
  font-family: var(--fgp-font-primary) !important;
  background: var(--fgp-orange-yellow) !important;
  color: var(--fgp-dark-cornflower-blue) !important;
  border-radius: var(--radius-pill) !important;
  font-weight: 700 !important;
  letter-spacing: 0.5px !important;
}

.shopify-payment-button__button:hover {
  opacity: 0.88 !important;
}

.product__description,
.product__description p,
.product__description li,
.product__description h2,
.product__description h3,
.product__description strong {
  font-family: var(--fgp-font-primary) !important;
  color: var(--fgp-white) !important;
  font-size: 14px !important;
  line-height: 1.6 !important;
}

/* ── Product page accordions (Dosage / Shipping / Return Policy) ── */
.product__accordion,
.product__accordion details,
.product__accordion summary {
  font-family: var(--fgp-font-primary) !important;
  background: transparent !important;
  border-color: rgba(255, 255, 255, 0.2) !important;
}

.product__accordion summary,
.product__accordion .accordion__title,
.product__accordion [id*="Collapsible"] ~ summary {
  font-family: var(--fgp-font-primary) !important;
  font-size: 16px !important;
  font-weight: 600 !important;
  color: var(--fgp-white) !important;
  padding: 16px 0 !important;
  border-bottom: 1px solid rgba(255, 255, 255, 0.2) !important;
}

.product__accordion .icon-caret,
.product__accordion summary svg {
  color: var(--fgp-white) !important;
  fill: var(--fgp-white) !important;
}

.product__accordion .accordion__content,
.product__accordion details > div,
.product__accordion [id*="CollapsibleAccordion"] {
  font-family: var(--fgp-font-primary) !important;
  font-size: 14px !important;
  font-weight: 400 !important;
  color: rgba(255, 255, 255, 0.8) !important;
  line-height: 1.6 !important;
  padding: 12px 0 16px !important;
}

.product__accordion .accordion__content p,
.product__accordion details > div p {
  color: rgba(255, 255, 255, 0.8) !important;
  font-family: var(--fgp-font-primary) !important;
}

/* ── Announcement bar (brand strip) ─────── */
.announcement-bar-section .utility-bar,
.utility-bar.color-scheme-4,
.utility-bar[class*="color-"] {
  background: var(--fgp-orange-yellow) !important;
  border-bottom: none !important;
}

.announcement-bar-section .announcement-bar__message,
.announcement-bar-section .announcement-bar__message span,
.announcement-bar-section .h5 {
  color: var(--fgp-dark-cornflower-blue) !important;
  font-family: var(--fgp-font-primary) !important;
  font-size: 14px !important;
  font-weight: 600 !important;
  letter-spacing: 0.6px !important;
}

/* ── Footer ──────────────────────────────── */
.footer {
  background: var(--fgp-dark-cornflower-blue) !important;
}

.footer,
.footer a,
.footer p,
.footer li,
.footer h2,
.footer-block__heading,
.footer__list-social-link {
  color: var(--fgp-white) !important;
  font-family: var(--fgp-font-primary) !important;
}

.footer a:hover {
  color: var(--fgp-picton-blue) !important;
}

.footer__newsletter input[type="email"] {
  border-color: rgba(255,255,255,0.3) !important;
  color: var(--fgp-dark-cornflower-blue) !important;
  font-family: var(--fgp-font-primary) !important;
}

.footer__newsletter button[type="submit"] {
  background: var(--fgp-orange-yellow) !important;
  color: var(--fgp-dark-cornflower-blue) !important;
  font-family: var(--fgp-font-primary) !important;
  font-weight: 700 !important;
}

.footer__content-bottom,
.footer__content-bottom p {
  color: rgba(255,255,255,0.6) !important;
  font-family: var(--fgp-font-primary) !important;
  font-size: 12px !important;
}

/* ── Global page content limit ─────────── */
.fgp-page-inner {
  max-width: 960px;
  margin: 0 auto;
  width: 100%;
  padding: 0 24px;
  box-sizing: border-box;
}

@media (min-width: 768px) {
  .fgp-page-inner { padding: 0 32px; }
}

@media (min-width: 1101px) {
  .fgp-page-inner { padding: 0 48px; }
}


/* ── Content container ─────────────────── */
.fgp-container {
  max-width: var(--content-max-width);
  margin: 0 auto;
  padding: 0 var(--section-px-desktop);
  width: 100%;
  box-sizing: border-box;
}

@media (max-width: 1100px) {
  .fgp-container {
    padding: 0 var(--space-40);
  }
}

@media (max-width: 767px) {
  .fgp-container {
    padding: 0 var(--section-px-mobile);
  }
}

/* ── Contact form — brand styling ─────────
   Dawn's colour scheme renders the fields in solid picton blue with white
   placeholder text, which fails contrast and reads as disabled. Fields are
   white with navy text; labels stay visible once a value is entered. */
/* Dawn paints inputs from the scheme's own custom properties, so those have to
   be neutralised at the source or the !important rules below still lose. */
.contact,
.contact .field,
.contact .field__input,
.contact .text-area {
  --color-background: 255, 255, 255;
  --color-foreground: 35, 64, 146;
}

.contact .contact__fields,
.contact form {
  max-width: 620px;
  margin: 0 auto;
}

/* Name + email share a row on desktop, stack on mobile */
.contact .contact__fields {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 16px;
}

.contact .contact__fields > .field:nth-child(n + 3),
.contact .contact__fields > .field:has(.text-area) {
  grid-column: 1 / -1;
}

@media screen and (max-width: 749px) {
  .contact .contact__fields {
    grid-template-columns: 1fr;
  }
}

/* The comment box was the same height as a single-line input */
.contact .text-area {
  min-height: 150px;
  resize: vertical;
}

.contact > h2,
.contact .title {
  font-family: var(--fgp-font-primary, 'Poppins', sans-serif) !important;
  color: var(--fgp-dark-cornflower-blue, #234092) !important;
  text-align: center;
  margin-bottom: 28px;
}

/* Dawn draws the visible outline with .field:after, not on the input itself.
   Styling the input's own border produced a second, offset outline, so the
   border lives on the pseudo-element and the input stays borderless. */
.contact .field:after,
.contact .select:after {
  border: 1.5px solid rgba(35, 64, 146, 0.22) !important;
  border-radius: 14px !important;
  box-shadow: none !important;
  transition: border-color 0.2s, box-shadow 0.2s;
}

.contact .field:focus-within:after {
  border-color: var(--fgp-picton-blue, #45bfef) !important;
  box-shadow: 0 0 0 3px rgba(69, 191, 239, 0.25) !important;
}

.contact .field__input,
.contact .text-area {
  background: var(--fgp-white, #fff) !important;
  color: var(--fgp-dark-cornflower-blue, #234092) !important;
  font-family: var(--fgp-font-primary, 'Poppins', sans-serif) !important;
  border: 0 !important;
  border-radius: 14px !important;
  box-shadow: none !important;
  outline: none !important;
  /* top padding leaves room for the label once it floats up */
  padding: 22px 20px 10px !important;
}

.contact .text-area {
  padding-top: 24px !important;
}

/* Dawn's floating label sits on top of the input and uses :placeholder-shown
   purely to detect an empty field — it assumes the placeholder itself is
   invisible. Showing it duplicated every label ("Nombre" over "Nombre"), so
   the placeholder is hidden and the label remains the visible one. */
.contact .field__input::placeholder,
.contact .text-area::placeholder {
  color: transparent !important;
  opacity: 0 !important;
}

/* focus is handled on .field:focus-within:after above */

/* The label floats inside the field, so it needs no backdrop of its own —
   adding one boxed it against the border. */
.contact .field__label {
  color: rgba(35, 64, 146, 0.55) !important;
  font-family: var(--fgp-font-primary, 'Poppins', sans-serif) !important;
  background: transparent !important;
  padding: 0 !important;
  letter-spacing: 0;
  /* match the input's 20px horizontal padding, not Dawn's 2rem */
  left: 20px !important;
}

.contact .field__input:not(:placeholder-shown) ~ .field__label,
.contact .text-area:not(:placeholder-shown) ~ .field__label,
.contact .field:focus-within .field__label {
  left: 20px !important;
}

/* Once the field has a value the label shrinks upward; keep it readable
   and give the text below it room */
.contact .field__input:not(:placeholder-shown) ~ .field__label,
.contact .text-area:not(:placeholder-shown) ~ .field__label {
  color: rgba(35, 64, 146, 0.7) !important;
  font-weight: 500;
}

/* Submit — navy pill, centred with the fields */
.contact .contact__button {
  display: flex;
  justify-content: center;
  margin-top: 8px;
}

.contact .contact__button .button {
  font-family: var(--fgp-font-primary, 'Poppins', sans-serif) !important;
  background: var(--fgp-dark-cornflower-blue, #234092) !important;
  color: var(--fgp-white, #fff) !important;
  border: none !important;
  border-radius: var(--radius-pill, 200px) !important;
  font-weight: 600 !important;
  letter-spacing: 0.5px !important;
  min-width: 220px;
  padding: 15px 34px !important;
  transition: opacity 0.2s, transform 0.2s !important;
}

.contact .contact__button .button:hover {
  opacity: 0.9 !important;
  transform: translateY(-1px);
}
</style>
